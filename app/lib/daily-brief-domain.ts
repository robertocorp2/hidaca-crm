export type DailyBriefFact = { kind: "overdue_activity" | "upcoming_activity" | "opportunity" | "quotation" | "overdue_invoice" | "project" | "case" | "recent_change"; entityType: string; entityId: string; title: string; date?: string | null; amount?: number | null; status?: string | null; updatedAt?: string | null };
export type DailyBriefSection = "today" | "risks" | "followups" | "collections" | "projects";
export type DailyBriefItem = { itemKey: string; section: DailyBriefSection; entityType: string; entityId: string; title: string; reason: string; priorityScore: number; suggestedAction: { type: "create_activity"; title: string; relatedType?: string; relatedId?: string; dueOffsetHours: number }; status: "active" | "dismissed" | "completed" };
function daysBetween(from: Date, to: Date) { return Math.floor((to.getTime() - from.getTime()) / 86_400_000); }
export function scoreDailyBriefFact(fact: DailyBriefFact, now = new Date()) { if (fact.kind === "overdue_invoice") { const days = fact.date ? Math.max(0, -daysBetween(now, new Date(fact.date))) : 0; return { score: Math.min(100, 92 + Math.min(days, 8)), reason: `Factura vencida${days ? ` hace ${days} día${days === 1 ? "" : "s"}` : ""} con balance pendiente.` }; } if (fact.kind === "overdue_activity") { const days = fact.date ? Math.max(0, -daysBetween(now, new Date(fact.date))) : 0; return { score: Math.min(99, 88 + Math.min(days, 10)), reason: `Actividad pendiente fuera de fecha${days ? ` por ${days} día${days === 1 ? "" : "s"}` : ""}.` }; } if (fact.kind === "opportunity") { const days = fact.date ? daysBetween(now, new Date(fact.date)) : null; return { score: days !== null && days <= 7 ? 84 : 74, reason: days === null ? "Oportunidad abierta sin fecha de cierre confirmada." : `Oportunidad abierta con cierre previsto en ${Math.max(days, 0)} día${days === 1 ? "" : "s"}.` }; } if (fact.kind === "quotation") return { score: 68, reason: "Cotización enviada que requiere seguimiento comercial." }; if (fact.kind === "project") { const stale = fact.updatedAt ? Math.max(0, -daysBetween(now, new Date(fact.updatedAt))) : 0; return { score: Math.min(70, 52 + Math.min(stale, 18)), reason: `Proyecto activo sin actualización reciente${stale ? ` (${stale} días)` : ""}.` }; } if (fact.kind === "case") return { score: 80, reason: "Caso operativo abierto que requiere revisión." }; if (fact.kind === "recent_change") return { score: 58, reason: "Cambio operativo reciente para validar en el contexto del día." }; return { score: 48, reason: "Actividad programada dentro de los próximos siete días." }; }
function sectionFor(fact: DailyBriefFact): DailyBriefSection { if (fact.kind === "overdue_invoice") return "collections"; if (fact.kind === "overdue_activity") return "risks"; if (fact.kind === "opportunity" || fact.kind === "quotation") return "followups"; if (fact.kind === "project") return "projects"; return "today"; }
export function buildDailyBriefItems(facts: DailyBriefFact[], now = new Date()): DailyBriefItem[] { const seen = new Set<string>(); return facts.flatMap((fact) => { const itemKey = `${fact.kind}:${fact.entityId}`; if (seen.has(itemKey)) return []; seen.add(itemKey); const priority = scoreDailyBriefFact(fact, now); const relatedType = ["business", "contact", "project", "lead", "opportunity", "case"].includes(fact.entityType) ? fact.entityType : undefined; return [{ itemKey, section: sectionFor(fact), entityType: fact.entityType, entityId: fact.entityId, title: fact.title, reason: priority.reason, priorityScore: priority.score, suggestedAction: { type: "create_activity" as const, title: `Seguimiento: ${fact.title}`.slice(0, 200), relatedType, relatedId: relatedType ? fact.entityId : undefined, dueOffsetHours: priority.score >= 88 ? 2 : priority.score >= 74 ? 24 : 72 }, status: "active" as const }]; }).sort((left, right) => right.priorityScore - left.priorityScore).slice(0, 40); }
